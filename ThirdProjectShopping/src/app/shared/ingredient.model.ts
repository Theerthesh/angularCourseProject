export class Ingredient{
// public name:string;
// public amount:number;

constructor(public name:string,public amount:number){
    // this.name = name;
    // this.amount = amount;
}

// adding public in constractor is =  to creating and assigning to variable is same
}

